document.addEventListener('DOMContentLoaded', () => {
  // This is the bare minimum JavaScript. You can opt to pass no arguments to setup.
  const player = new Plyr('#player');

  player.ads = {
    enable:true,
    publisherId:'122432164390505'
  };

  //console.log(player);
});
